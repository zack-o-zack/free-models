# Domain documentation

This is a single-context repository.

Before working in the codebase, read the root `CONTEXT.md` and relevant records under `docs/adr/` when they exist. Use the glossary terms defined in `CONTEXT.md`. If an implementation conflicts with an architectural decision record, surface the conflict instead of silently overriding it.

The domain documentation may be created lazily as the model catalogue domain develops.
